{
  "name": "-examples",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "example": "pnpm prettier ./input --write --plugin prettier-plugin-ember-template-tag",
    "example-debug": "pnpm prettier ./input --write --plugin prettier-plugin-ember-template-tag --log-level debug",
    "test": "node ./bin/test.mjs"
  },
  "dependencies": {
    "prettier": "^3.1.0",
    "prettier-plugin-ember-template-tag": "workspace:^"
  },
  "devDependencies": {
    "chalk": "^5.3.0"
  }
}
